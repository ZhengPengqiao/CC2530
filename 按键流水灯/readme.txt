KEY.c文件是关于按键的代码，对S1、S2按键进行了选择编译，仅编译相关的代码。
int key*Value()仅仅读取按键的值，没有去抖
int key*Scan() 读取按键的状态，已经经过去抖

KEY.h是对Key资源的定义，并且选择编译那一部分代码也是在这里设置，
#define S1 1   //使用按键KEY1
#define S2 1   //使用按键KEY2

main.c 记录闪烁状态，没按键一下，翻转一下按键状态，通过闪烁状态，来进行时闪烁还
是熄灭。